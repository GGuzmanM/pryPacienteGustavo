<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AppPaciente" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/letra</item>

    </style>

    <style name="Theme.AppPaciente" parent="Base.Theme.AppPaciente" />

    <style name="Theme.MyApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="menuItemColor">@color/menu_item_color</item>
        <!-- Otros atributos de tema -->
    </style>
</resources>